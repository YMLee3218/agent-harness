<!--
INSTRUCTIONS FOR USE:
1. Copy this file to .claude/commands/local-<your-name>.md
2. Replace the content below with your command prompt
3. The slash command name will be /local-<your-name>

TIPS:
- The entire file content becomes the prompt when the command is invoked
- Use $ARGUMENTS to capture text typed after the command name
  e.g. /local-mycommand some text  →  $ARGUMENTS = "some text"
- Reference other files with @path/to/file.md inside the prompt
- Keep commands focused on a single task
-->

Describe what this command does. $ARGUMENTS
