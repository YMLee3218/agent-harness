Run integration tests and optionally roll back to an earlier phase if they fail.

Delegates to the `running-integration-tests` skill. See `skills/running-integration-tests/SKILL.md` for full documentation.
